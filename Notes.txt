API's:
=====

1. Vendor
---------
✅    1. Create -> localhost:5000/vendor/create 
✅    2. Login -> localhost:5000/vendor/login 
✅    3. GetAllVendors -> localhost:5000/vendor/getall 
✅    4. Get one vendor -> localhost:5000/vendor/get/:id
✅    5. Update vendor -> localhost:5000/vendor/update/:id
    6. delete vendor -> localhost:5000/vendor/delete/:id


2. outlet
---------
✅    1. Create -> localhost:5000/outlet/create  <=====>   token needed.
✅    2. All outlets -> localhost:5000/outlet/getall
✅    3. Single Outlet ->localhost:5000/outlet/get/:id
✅    4. outlets by Vendor -> localhost:5000/outlet/vendor/:vendorid
✅    5. Update -> localhost:5000/outlet/update/:id  <=====>   token needed.
    6. delete -> localhost:5000/outlet/delete/:id  <=====>   token needed.

3. Menu-Category
----------------
✅    1. Create -> localhost:5000/menu-category/create/:outletId
✅    2. MenuCategories by Outlet -> localhost:5000/menu-category/outlet/:outletid
✅    3. All Categories -> localhost:5000/menu-category/getall
✅    4. Single Category -> localhost:5000/menu-category/get/:id 
✅    5. Update -> localhost:5000/menu-category/update/:id 
✅    6. delete -> localhost:5000/menu-category/delete/:id 


4. MenuItem
------------
✅    1. Create -> localhost:5000/menu-item/add/:categoryid
✅    2. All Items -> localhost:5000/menu-item/getall
✅    3. Items by Outlet -> localhost:5000/menu-item/outlet/:outletid 
✅    4. Items by Category -> localhost:5000/menu-item/category/:categoryid
✅    5. single Item -> localhost:5000/menu-item/get/:meuItemId
✅    6. update -> localhost:5000/menu-item/update/:munuItemId
✅    7. delete -> localhost:5000/menu-item/delete/:menuItemId.


https://foodstack-backend-fr5y.onrender.com/


Forgot password
===============
Vendor clicks "Forgot Password"
        ↓
Enters registered email
        ↓
Backend finds Vendor
        ↓
Generate secure random reset token
        ↓
Store HASHED token + expiry in MongoDB
        ↓
Send reset link through email
        ↓
User clicks link
        ↓
React opens /reset-password/:token
        ↓
User enters new password and confirm password
        ↓
Backend hashes token and verifies it
        ↓
Update password
        ↓
Delete reset token
        ↓
User logs in with new password

// Implementing Redis cloud

cache keys
1. getting all outlets 
        -> outlets:all
        -> it might contain 
                ->
                [
                        Outlet 1,
                        Outlet 2,
                        Outlet.3
                        .......
                ]
2. get outlet by id 
        -> outlet:{id}
3. get outlets by vendor
        -> outlets:vendor:{vendorId}





Redis Cache keys:
----------------
1. outlet
---------
        ->`vendor:session:${vendorId}`
        ->"outlets:all"
        ->`outlet:${outletId}`
        ->`outlets:vendor:${vendorId}`
2. menuCategory
        ->"menuCategories:all"
        ->`menuCategories:outlet:${outletId}`
        ->`menuCategory:${menuCategoryId}`
        ->`menuCategories:vendor:${vendorId}`
3.menuItems
        ->"meniItems:all"
        ->`meniItem:${menuItemId}`
        ->`menuItems:outlet:${outletId}`
        ->`menuItems:category:${categoryId}`
        ->`menuItems:vendor:${vendorId}`


Making FoodStack Distributed system.

1. Make Application stateless.
        ->Any backend instance should be able to handle any request without depending on data stored inside that particular Node.js process.

        What we're trying to achieve

        After Step 1, your FoodStack should look conceptually like this:
                   FOODSTACK
                       │
                       ↓
                  React Client
                       │
                       ↓
                 Node/Express
                       │
            ┌──────────┼──────────┐
            ↓          ↓          ↓
         MongoDB     Redis    Cloudinary
            │          │          │
         Data       Sessions     Images

Step 2. 
You can use this as your checklist:

Data	                        Where it should live
Vendor	                        MongoDB
//Customer	                MongoDB
Restaurant	                MongoDB
Outlet	                        MongoDB
Product	                        MongoDB
//Menu	                        MongoDB
//Orders	                        MongoDB
//Payment record	                MongoDB
Login session	                Redis
Cache	                        Redis
Rate-limit counters	        Redis
Profile images	                Cloudinary
Restaurant images	        Cloudinary
Product images	                Cloudinary
Email delivery	                Email provider
JWT	                        Client
Secrets/config	                Environment variables

step 3 ; Create a health-check endpoint - just texting with intial route with "..../health"
step 4 : Deploy your backend
step 5 : Run multiple backend instances
Currently we probably have:

                    Render
                      │
                      ↓
                FoodStack Backend
                      │
              ┌───────┴────────┐
              ↓                ↓
           MongoDB           Redis

We want:

                    Render
                      │
              ┌───────┼───────┐
              ↓       ↓       ↓
          Instance 1 Instance 2 Instance 3
              │       │       │
              └───────┼───────┘
                      ↓
               Same MongoDB
               Same Redis
               Same Cloudinary

        1. Make your FoodStack server accept a configurable port
                const PORT = process.env.PORT || 5000;

                app.listen(PORT, () => {
                console.log(`Server running on ${PORT}`);
                });
                Backend #1 → 5001
                Backend #2 → 5002
                Backend #3 → 5003
        2. Create an instance ID and run .
                -> run each 
                        ->$env:PORT=5001; node index.js
                        ->$env:PORT=5002; node index.js
                        ->$env:PORT=5003; node index.js

step 6: create loadBalencer
                         React
                           │
                           │
                           ▼
                  ┌─────────────────┐
                  │  Load Balancer  │
                  │      :4000      │
                  └────────┬────────┘
                           │
                Round Robin Algorithm (selsecting the backend server from the array)
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
         ┌─────────┐  ┌─────────┐  ┌─────────┐
         │Backend 1│  │Backend 2│  │Backend 3│
         │  :5001  │  │  :5002  │  │  :5003  │
         └────┬────┘  └────┬────┘  └────┬────┘
              │            │            │
              └────────────┼────────────┘
                           ▼
                       MongoDB
                           │
                         Redis
                           │
                       Cloudinary

The problem

Every request creates a new proxy middleware:
fix:
creating Proxy instance ONCE per each packen.


loadBalencer:
============
                    START
                      │
                      ▼
              Load Balancer starts
                      │
                      ▼
            Load environment variables
                      │
                      ▼
              Read backend URLs
                      │
                      ▼
       ┌──────────────────────────────┐
       │ Create serverStatus[]        │
       │                              │
       │ 5001 → healthy: true         │
       │ 5002 → healthy: true         │
       │ 5003 → healthy: true         │
       └──────────────┬───────────────┘
                      │
                      ▼
             Create proxy for
             each backend server
                      │
                      ▼
              Start health check
                      │
                      ▼
          Check 5001, 5002, 5003
                      │
                      ▼
             Update serverStatus
                      │
                      ▼
            Start Express server
                      │
                      ▼
          Wait for client request
                      │
                      ▼
             ┌─────────────────┐
             │ Client Request  │
             │ GET /products   │
             └────────┬────────┘
                      │
                      ▼
             Load Balancer
                receives
                request
                      │
                      ▼
          getNextHealthyServer()
                      │
                      ▼
          ┌─────────────────────┐
          │ Check currentServer │
          └──────────┬──────────┘
                     │
                     ▼
              Is server healthy?
                /           \
              YES            NO
               │              │
               │              ▼
               │       Check next server
               │              │
               │              ▼
               │       Is it healthy?
               │          /       \
               │        YES        NO
               │         │          │
               ▼         │          ▼
         Select server   │    Continue checking
               │         │
               └────┬────┘
                    │
                    ▼
             Move pointer
          to next server
                    │
                    ▼
            Forward request
              using proxy
                    │
                    ▼
          ┌──────────────────┐
          │ Selected Backend │
          │                  │
          │ 5001 / 5002 /    │
          │ 5003             │
          └────────┬─────────┘
                   │
                   ▼
             Backend handles
                request
                   │
                   ▼
             Backend sends
                response
                   │
                   ▼
             Proxy forwards
               response
                   │
                   ▼
                 Client


Testing
============
npx jest tests/vendor.test.js --runInBand -- used to run only one file.